@model List<EmpleadoAdminModelo>
@{
    ViewData["Title"] = "Listar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section head{
    <style type="text/css">
        body {
            background-image: url('/images/fondo.png');
            margin: 0;
        }
    </style>
}

    <div class="card">
        <div class="card-header">
            Listado de Empleados Administrativos
        </div>
        <div class="card-body">
            <a asp-action="Guardar" asp-controller="EmpleadoAdmin" class="btn btn-success"><i class="bi bi-plus-circle"></i>Crear Nuevo</a>
            <hr />
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>Identificacion</th>
                        <th>Nombres</th>
                        <th>Apellidos</th>
                        <th>Fecha de Nacimiento Nacimiento</th>
                        <th>Teléfono</th>
                        <th>Nivel Estudio</th>
                        <th>Email</th>
                        <th>Cargo</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Identificacion</td>
                        <td>@item.Nombres</td>
                        <td>@item.Apellidos</td>
                        <td>@item.FechaNacimiento</td>
                        <td>@item.Telefono</td>
                        <td>@item.NivelEstudio</td>
                        <td>@item.Email</td>
                        <td>@item.Cargo</td>
                        <td>
                            <a asp-action="Editar" asp-controller="EmpleadoAdmin" class="btn btn-primary btn-sm" asp-route-IdAdministrativo="@item.IdAdministrativo"><i class="bi bi-pencil-square"></i>Editar</a>
                            <a asp-action="Eliminar" asp-controller="EmpleadoAdmin" class="btn btn-danger btn-sm" asp-route-IdAdministrativo="@item.IdAdministrativo"><i class="bi bi-x-octagon"></i>Eliminar</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

